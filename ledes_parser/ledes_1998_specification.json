{
  "format": "1998",
  "fields": [
    {
      "label": "INVOICE_DATE",
      "position": 1,
      "type": "date",
      "required": true,
      "description": "The invoice date. A null value (\"\") would be an error.",
      "notes": "For each INVOICE_NUMBER, only the first INVOICE_DATE appearing for that INVOICE_NUMBER is valid (i.e., all others after the first can be disregarded)."
    }
  ]
}